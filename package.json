{
  "name": "prepmind-ai",
  "version": "1.0.0",
  "type": "module",
  "description": "Free AI-powered test prep for SAT, ACT, and AP exams",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build:client": "vite build",
    "build:server": "echo \"(server is JS; no compile step)\"",
    "build": "npm run build:client && npm run build:server",
    "start": "node server/index.js",
    "preview": "node server/index.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "together-ai": "^0.10.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
